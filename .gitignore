node_modules/
dist/
.env
.env.*
*.log
.DS_Store

# never commit real credentials (the talent-night copy shipped an environment.txt)
environment.*
!.env.example

# talent-night donor copy kept locally for reference, not part of the build
ptci_tabulation/
